<% if signed_in? %>
  $("#sign_in_modal").modal('hide');
  $("#main_navigation").html("<%= j render('layouts/navigation') %>");
  window.location = $(".redirect-to").attr('href');
<% else %>
  // should not get here, the failure action should kick in
  alert("Failed to log in. Please try again!");
<% end %>